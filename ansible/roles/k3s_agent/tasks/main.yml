---
- name: Validate required variables
  ansible.builtin.assert:
    that:
      - k3s_server_url | length > 0
      - k3s_node_token | length > 0
    fail_msg: "k3s_server_url and k3s_node_token must be set (passed from k3s_server role or group_vars)"

- name: Check if k3s agent is already installed
  ansible.builtin.stat:
    path: /usr/local/bin/k3s
  register: k3s_binary

- name: Get installed k3s version
  ansible.builtin.command: k3s --version
  register: k3s_installed_version
  changed_when: false
  failed_when: k3s_installed_version.rc not in [0, 1, 2, 127]
  when: k3s_binary.stat.exists

- name: Download k3s installer
  ansible.builtin.get_url:
    url: https://get.k3s.io
    dest: /tmp/k3s-install.sh
    mode: "0700"
  when: >
    not k3s_binary.stat.exists or
    k3s_version not in (k3s_installed_version.stdout | default(''))

- name: Install k3s agent
  ansible.builtin.command:
    cmd: /tmp/k3s-install.sh
  no_log: true
  environment:
    INSTALL_K3S_VERSION: "{{ k3s_version }}"
    K3S_URL: "{{ k3s_server_url }}"
    K3S_TOKEN: "{{ k3s_node_token }}"
    INSTALL_K3S_EXEC: >-
      agent
      --node-ip {{ k3s_node_ip }}
      {{ k3s_extra_agent_args }}
  when: >
    not k3s_binary.stat.exists or
    k3s_version not in (k3s_installed_version.stdout | default(''))
  changed_when: true
  notify: Restart k3s-agent

- name: Wait for k3s-agent service to be active
  ansible.builtin.systemd:
    name: k3s-agent
    state: started
    enabled: true

- name: Open firewall ports for cluster communication
  community.general.ufw:
    rule: allow
    port: "{{ item.port }}"
    proto: "{{ item.proto }}"
    from_ip: "{{ k3s_ufw_allow_from }}"
  loop:
    - { port: "8472", proto: udp }
    - { port: "10250", proto: tcp }
  notify: Reload UFW

- name: Open ingress ports (HTTP/HTTPS) with rate limiting
  community.general.ufw:
    rule: limit
    port: "{{ item }}"
    proto: tcp
  loop:
    - "80"
    - "443"
  notify: Reload UFW
