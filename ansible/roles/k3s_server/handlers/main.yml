---
- name: Restart k3s
  ansible.builtin.systemd:
    name: k3s
    state: restarted
    daemon_reload: true

- name: Reload UFW
  community.general.ufw:
    state: reloaded

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
