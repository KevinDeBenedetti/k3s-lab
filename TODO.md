# TODO

## 🔴 En cours

## 🟡 À faire

- [ ] FIX(cluster): la StatefulSet `vault` déclare `hashicorp/vault:1.21.4` mais le pod `vault-0` tourne toujours en **1.21.2** — dérive re-confirmée le 2026-08-15 (`OnDelete`, `current=vault-648b66f478` ≠ `update=vault-6bb68f4f65`, `updatedReplicas` vide, `ready=1`). **La question posée est tranchée : 1.21.4 ne corrige AUCUN des CRITICAL, donc l'opération risquée n'achète rien côté CVE.** Le VulnerabilityReport de `statefulset-vault-vault` porte sur `hashicorp/vault:1.21.4` (Trivy scanne le spec, pas le pod) et compte toujours **5 CRITICAL / 54 HIGH** : CVE-2026-31789 (`libcrypto3`/`libssl3` 3.5.5-r0, corrigé en 3.5.6-r0 — image de base Alpine), CVE-2026-33815 + CVE-2026-33816 (`pgx/v5` 5.7.6 → 5.9.0), CVE-2026-33186 (`grpc` 1.75.1 → 1.79.3). Tous exigent une **nouvelle build amont**, pas un bump 1.21.2 → 1.21.4. Attendre une release Vault dont la base porte openssl 3.5.6-r0 et les deps Go à jour, plutôt que de dépiler `vault-0` (donc un unseal) pour rien. **Bloqué de toute façon** : les verbes mutants de `kubectl` sont refusés à l'agent par hook (vérifié, pas supposé), et c'est une opération de production
- [ ] FIX: `portfolio/purge` échoue en **401** à chaque run (04:00 UTC). **Diagnostic corrigé le 2026-08-15 — rien à faire côté Vault/ESO, le secret est sain** : `WORKER_SECRET` est une valeur propre de 21 caractères, sans espace ni retour à la ligne, synchronisée (`SecretSynced`), et lue depuis la **même** clé du même Secret par le Deployment (`envFrom`) et par le CronJob (`secretKeyRef`). Vraie cause : l'image déployée est `portfolio:v0.21.0` (tag du 2026-04-13) et la route `/api/maintenance/purge` n'a été ajoutée que le 2026-06-29 (commit `5d3fa4f`, **non ancêtre de v0.21.0**) — elle n'existe pas dans l'image qui tourne. Le 401 (et non un 404) vient de `middleware.ts` de v0.21.0, qui intercepte tout `/api/*` hors `/api/auth` et `/api/health` et renvoie `{"error":"Unauthorized"}` en 401 avant le routage, faute de session NextAuth — un Bearer worker ne lui parle pas. **Bloqué en amont** : `v0.21.0` est le dernier tag de `portfolio-next`, donc aucune image publiée ne contient la route ; il faut d'abord couper une release de `portfolio-next`, puis bumper le pin dans `infra/apps/portfolio/deployment.yaml:30` (tag **et** digest). Hors des deux dépôts, non appliqué
- [ ] FIX(cluster): les 16 restarts de `portfolio-7b77fcbfb4-ng6kj` sont un **OOM, sans rapport avec `WORKER_SECRET`** (l'hypothèse « les deux pointent vers le même secret » est infirmée) : `exitCode: 137`, et le pod consomme 243Mi pour une limite de 256Mi — 95 %, il se fait tuer dès qu'il respire. `NODE_OPTIONS=--max-old-space-size=128` ne borne que le old-space V8, pas le RSS. Correctif = relever `resources.limits.memory` dans `infra/apps/portfolio/deployment.yaml:65`, mais dimensionner sur une mesure et non au jugé. Hors du dépôt courant et décision de capacité de production

## 🟢 Idées / backlog

## 🤖 Claude — recommandations
- [ ] CHORE: câbler `check-kyverno-crds.sh` dans un workflow disposant du kubeconfig (à côté de `check-deployed-charts.sh` dans `traefik-advisories.yml`, ou un job planifié dédié) — le script existe mais rien ne le déclenche, donc il ne verra la prochaine panne que si quelqu'un pense à le lancer
- [ ] FIX: `GITHUB_TOKEN` et `UPSTASH_REDIS_REST_TOKEN` valent littéralement `placeholder` dans `secret/portfolio/prod` (même sha256, 11 octets) — repéré en diagnostiquant le purge ; le rate-limit Upstash du middleware est donc silencieusement inopérant
- [ ] TEST: aucun check ne relie un CronJob à la version d'app qui expose l'endpoint qu'il appelle — le purge tapait une route absente de l'image depuis sa création, et rien n'a rien dit pendant des semaines
- [ ] CHORE: migrer `infra/platform/security/values.yaml:51` de `kyvernoPolicies.validationFailureAction` vers `failureAction`, puis retirer l'alias déprécié du helper — l'alias n'existe que pour ce consommateur, et le test « the deprecated validationFailureAction value key still overrides » est là pour rendre sa suppression délibérée

## ✅ Fait
