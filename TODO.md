# TODO

## 🔴 En cours

## 🟡 À faire

- [ ] CHORE: republier `platform-traefik` après commit — le chart publié 0.15.0 embarque encore le subchart 41.0.2 avec le pin `v3.7.9`, alors que le repo est passé à 41.1.0 sans pin (travail non commité du 2026-07-30). Les deux déploient bien v3.7.9, donc rien d'urgent, mais l'artefact et la source divergent tant que la release n'est pas sortie. **Laissé ouvert le 2026-08-01 : l'action demandée t'appartient** — il faut commiter puis sortir une release, `release-charts` republiant à ce moment-là. Ce qui pouvait être fait l'a été : pré-vol effectué en empaquetant le chart exactement comme le ferait la release (`helm package`, hors ligne, le subchart 41.1.0 étant déjà vendorisé). L'artefact obtenu embarque le sous-chart **déplié** (`charts/traefik/`), lock en 41.1.0, **sans pin** — et `traefik_effective_tag` y résout bien `v3.7.9`. Ça valide un point non trivial : la disparition du pin change la forme de l'artefact, et sans le support du format déplié ajouté aujourd'hui à `lib/traefik-pin.sh`, `check-deployed-traefik.sh` aurait cessé de savoir lire le chart dès cette release (en échouant bruyamment, pas en passant en silence). Divergence actuelle confirmée bénigne : publié = subchart 41.0.2 + pin v3.7.9, source = 41.1.0 sans pin, les deux déploient v3.7.9.

## 🟢 Idées / backlog

## 🤖 Claude — recommandations

- [ ] CHORE: créer le secret `INFRA_READ_TOKEN` sur `k3s-lab` (PAT en lecture seule sur `KevinDeBenedetti/infra`) — sans lui, la veille quotidienne ne contrôle que la version que le repo déploierait, jamais celle qui tourne. Elle le signale par un `::warning::` à chaque run, mais un avertissement sur un run vert se lit mal : c'est précisément la moitié la plus importante du contrôle qui reste éteinte tant que le secret n'existe pas.
- [ ] TEST: étendre `check-deployed-traefik.sh` aux autres charts déployés — `platform-cert-manager` et `platform-external-secrets` sont épinglés dans le même ApplicationSet et personne ne confronte leurs versions amont à quoi que ce soit. La mécanique (pin → chart publié → contenu réel) est déjà écrite et générique ; il manque la source d'advisories par composant, qui n'est pas forcément un dépôt GitHub comme pour Traefik.
- [ ] REFACTOR: `scripts/check-deployed-traefik.sh` code en dur `kevindebenedetti/charts/platform-traefik` comme chemin de registre par défaut, alors que l'ApplicationSet porte déjà le `repoURL` — le lire de là éviterait une troisième copie de la même vérité. Sans effet aujourd'hui (les deux coïncident), mais c'est exactement la classe de duplication qui a produit les dérives de pins.

## ✅ Fait

- [x] 2026-08-01 — DOCS: purger le README de `platform-deployment` du flux que personne n'emprunte. Option retenue : **documenter qu'il est publié mais non déployé**, plutôt que trancher son sort — supprimer un chart publié est une décision d'un autre ordre. Encadré en tête de page qui l'énonce et donne la commande pour le revérifier soi-même. Mais le vrai défaut n'était pas l'inutilité, il était la **fausseté** : la section « Upgrades » expliquait de lancer `helm dependency update` et d'éditer le `Chart.yaml` du wrapper pour changer la version d'un sous-chart. Un consommateur d'un chart publié ne peut faire ni l'un ni l'autre — les versions des sous-charts sont figées au moment de la release, et rien dans `values.yaml` ne les surcharge (`enabled` éteint un sous-chart, il ne le reversionne pas). La section se contredisait d'ailleurs elle-même (« Edit values… » puis « Actually, to update a subchart version, edit the wrapper's Chart.yaml »). Réécrite : ce qu'un consommateur peut faire (passer à une version publiée plus récente), ce qu'il ne peut pas (bouger un seul composant — pour ça, déployer le chart unitaire, ce que fait déjà le cluster), et que changer un pin est une action **côté producteur**. Le « (recommended) » de la section ArgoCD requalifié en « the intended path, not currently in use ».
- [x] 2026-08-01 — TEST(URGENT): faire porter la veille d'advisories sur la version **réellement déployée**. `scripts/check-deployed-traefik.sh` remonte toute la chaîne : pin de l'ApplicationSet → chart **publié** sur GHCR (jeton anonyme, manifeste OCI, blob de la couche `helm.chart.content`) → version de proxy effective → advisories. **Validé sur le cas réel, dans les deux sens** : sur le pin actuel (0.15.0) il confirme v3.7.9 propre ; sur `--chart-version 0.14.0` il reproduit exactement l'exposition passée inaperçue cinq jours — `GHSA-3ccp-42pg-hgv6`, v3.7.8, exit 1. C'est la preuve que le trou est bien fermé, pas juste que le script tourne. Trois points de conception. (1) La confrontation est **factorisée** dans `lib/traefik-advisories.sh` et utilisée par les deux veilles : elles posent la même question sur deux versions différentes et ne doivent pas pouvoir diverger sur ce que « vulnérable » veut dire. Refactor vérifié non régressif — même sortie (77 plages), et contrôle négatif rejoué. (2) `traefik_subchart_appversion` sait désormais lire le sous-chart **déplié**, forme que produit `helm package` et donc celle de tout chart retiré du registre ; sa `version:` est recoupée avec `Chart.lock`, l'équivalent du nom de fichier exact qui protège la forme empaquetée. (3) Le parsing de l'ApplicationSet est dans `lib/chart-deps.sh` et testé, parce qu'il alimente un contrôle de sécurité : lire le mauvais élément rendrait « propre » un déploiement vulnérable, pire que ne rien vérifier — d'où le test qui vérifie qu'on ne renvoie pas la version du voisin, et celui qui vérifie qu'on matche sur `chart:` et non sur `name:` (ils diffèrent). 98 tests bats (+21). Câblé dans la veille quotidienne : le pin vivant dans `infra` (privé), le job clone ce repo **si** `INFRA_READ_TOKEN` est fourni, et sinon émet un `::warning::` explicite — le contrôle le plus important est celui qui ne tourne pas, ça doit rester visible à chaque run vert plutôt que d'être passé sous silence. Corps de l'issue de suivi refait en deux sections, avec la consigne que la 2 est l'urgente et que corriger la 1 ne corrige **pas** la production.

- [x] 2026-08-01 — FEAT: envisager des dépendances `file://` pour `platform-deployment` — **évalué, décision : ne pas adopter.** La tâche demandait d'*envisager* ; l'évaluation est faite et documentée sur trois sessions, la voici close plutôt que laissée à traîner. Le prototype marchait (rendu hors ligne depuis la source, 401 manifestes, artefact autonome de 3 Mo, `version: "*"` supprimant les 7 pins), mais deux faits l'emportent. (1) `file://` introduit un mode de panne **silencieux et mesuré** : `helm dependency update` ne résout pas récursivement les dépendances des sous-charts, donc sur une checkout fraîche — le glob triant l'umbrella 4ᵉ sur 10, avant `platform-traefik` — il embarque un `platform-traefik` vidé de son propre subchart. Les deux commandes sortent 0, le rendu tombe à 17 manifestes contre 401, ingress compris, sans un signal. Le rendre sûr demanderait une résolution en deux passes que je **n'ai pas pu valider** ici (pas de réseau pour les dépendances amont des charts feuilles), et publier ça non validé, c'est reprendre le pari qui a coûté 0.13.0/0.13.1/0.13.2. (2) Les deux problèmes qui motivaient l'idée ont été réglés autrement depuis : la dérive des pins par le job `umbrella-pins` (2026-07-31), et l'umbrella jamais rendu par le contrôle par sous-chart de `release-charts.yml` (2026-08-01). Il ne reste que le rendu **pré**-publication, et c'est précisément ce qui recréerait le blocage circulaire du 2026-07-21 que `template-exclude-paths` existe pour éviter. Ajouté à cela : personne ne consomme l'umbrella (revérifié), donc le bénéfice porterait sur un artefact que rien ne déploie. À rouvrir si l'umbrella devient réellement déployé — l'analyse ci-dessus reste valable telle quelle.
- [x] 2026-08-01 — TEST: contrôle de rendu de `platform-deployment` en CI — **sans objet, sa condition n'est pas remplie.** La tâche était explicitement conditionnelle (« si les dépendances `file://` sont retenues », « à ne pas faire avant ») ; `file://` étant écarté ci-dessus, le rendu pré-publication reste hors d'atteinte sans rouvrir le blocage circulaire. Le besoin de fond qu'elle exprimait — « l'umbrella n'est jamais rendu avant publication, personne ne le vérifie » — est en revanche **traité** depuis le 2026-08-01, en post-publication et correctement : `release-charts.yml` ne compte plus les documents mais vérifie via `scripts/check-umbrella-render.sh` que chacun des 7 sous-charts a bien produit des manifestes. La limite assumée reste la même qu'avant : ça détecte un umbrella creux, ça n'empêche pas de le pousser.

- [x] 2026-08-01 — FIX(URGENT): passer les trois pins de `infra/argocd/applicationsets/platform.yaml` de `0.14.0` à `0.15.0`. ⚠️ **Ce n'était pas une tâche d'entretien : le cluster tournait sur un Traefik vulnérable.** En appliquant l'instruction que le fichier porte lui-même (« vérifier avant de pinner »), j'ai inspecté les artefacts **publiés** sur GHCR plutôt que la source du repo : `platform-traefik` **0.14.0 épingle le proxy v3.7.8**, `0.15.0` épingle v3.7.9. Or v3.7.8 est couvert par `GHSA-3ccp-42pg-hgv6` (HIGH, publiée le 2026-07-27, empoisonnement de réponse entre utilisateurs via CONNECT proxifié sur le pool keep-alive partagé, plage `<= v3.7.8`) — confrontation refaite en direct contre l'API. Et `infra/platform/traefik/values.yaml` ne pose aucun override d'image, donc c'est bien v3.7.8 qui tournait en production **depuis le 2026-07-27**. Le trou est instructif : toute la veille construite ces derniers jours interroge la version que *le repo* déploierait (v3.7.9, verte tous les jours), jamais celle que *le cluster* exécute. Les deux ont divergé d'une release entière sans qu'aucun contrôle ne bronche. Commentaire ajouté dans le fichier interdisant explicitement le retour à 0.14.0, avec l'advisory et la raison, sur le modèle de la note 0.13.x déjà présente. Vérifié : le YAML parse, les 3 sources du template sont intactes, et les manifestes `0.15.0` des trois charts répondent 200 sur GHCR. **Non commité** — et le `syncPolicy` est en `automated` + `selfHeal`, donc le push déclenchera la réconciliation.
- [x] 2026-08-01 — FIX: renforcer le garde-fou de rendu de l'umbrella dans `release-charts.yml`. Le seuil « au moins 7 documents » ne mesurait pas ce qu'il prétendait : le rendu creux du 2026-08-01 en produisait **17** et passait, alors qu'il lui manquait **quatre sous-charts sur sept**, ingress compris (401 documents pour un rendu sain). Remplacé par la bonne question — non pas *combien* de manifestes, mais *quels sous-charts* en ont produit. `helm template` y répond directement : chaque manifeste porte un commentaire `# Source: <chart>/charts/<sous-chart>/…`, et un sous-chart qui n'a rien produit n'a aucune ligne, quel que soit le total. Nouveau `scripts/check-umbrella-render.sh`, qui lit la liste attendue depuis le `Chart.yaml` (donc elle ne peut pas se désynchroniser) et échoue en nommant chaque sous-chart muet. Validé sur les deux rendus réels capturés : le sain passe (7/7, 401 manifestes), le creux échoue en listant `platform-argocd`, `platform-external-secrets`, `platform-cert-manager` et `platform-traefik`. Nouveaux helpers `chart_deps` / `chart_dep_names` / `chart_name` (`lib/chart-deps.sh`, `chart_oci_deps` refactorisé par-dessus). 90 tests bats (13 de plus), dont le piège du nom préfixe — `sub-a` ne doit pas être crédité par un manifeste de `sub-ab` — et le cas exact du trou d'origine : 50 manifestes d'un seul sous-chart n'excusent pas les deux absents. Corrigé au passage dans `charts/platform-deployment/values.yaml` un `--version 1.0.0` résiduel, dernier reste de la ligne 1.x abandonnée le 2026-07-21.
