# TODO

## 🔴 En cours

## 🟡 À faire
- [ ] CHORE: `.ansible-lint` exclut `inventory/` mais rien ne vérifie que l'inventaire réel reste bien gitignored — un `hosts.yml` committé par accident exposerait les IP des nœuds et passerait tous les checks actuels
- [ ] TEST: `check-play-order.sh` n'assure que l'ordre des rôles ; rien ne vérifie que `ufw_enable` porte bien sur TOUS les hôtes touchés par les rôles de règles — un play final en `hosts: k3s_servers` laisserait les agents avec des règles jamais activées
- [ ] CHORE: alternative plus simple à la PolicyException, si vous préférez la porter en amont — ajouter `ci` à la liste d'exclusion des ClusterPolicies du chart platform-security, qui contient déjà 8 namespaces d'infrastructure (`kube-system`, `kyverno`, `argocd`, `vault`, `monitoring`, `external-secrets`, `cert-manager`, `ingress`) auxquels `ci` s'apparente. Supprimerait le besoin du feature flag et du manifeste d'exception. ⚠ Dépôt k3s-lab.

- [ ] FIX: corrige cette issue https://github.com/KevinDeBenedetti/k3s-lab/issues/76

## 🟡 À faire (manuellement, étapes détaillées pas à pas)
- [ ] CHORE: armer le `schedule:` de `.github/workflows/kyverno-crds.yml` — **la moitié k3s-lab est faite (2026-08-19)**, il ne reste que la dépendance infra. Le workflow cible désormais `runs-on: k3s-lab` (label déclaré dans `.github/actionlint.yaml`), n'utilise plus aucun secret `KUBECONFIG` — le runner tournant dans le cluster, `kubectl` s'authentifie via le ServiceAccount du pod — et installe `kubectl` lui-même, l'image ARC ne le fournissant pas contrairement à `ubuntu-latest`. Le `schedule` reste **commenté** parce qu'un job planifié visant un runner non enregistré n'échoue pas : il reste *queued* jusqu'au timeout de 6 h, tous les jours — plus silencieux qu'un rouge. Débloquer dans l'ordre : 1) créer le scale set ARC `k3s-lab` côté infra (entrée dédiée dans `infra/TODO.md`, manifeste RBAC prêt) ; 2) lancer une fois en `workflow_dispatch` et vérifier que le job démarre, que `kubectl version --client` passe et que le check lit bien les policies ; 3) seulement ensuite décommenter le `cron`.
- [ ] TEST: le job CI `k3s-lab-pin` n'a jamais tourné (ajouté depuis un sandbox sans accès Actions) — surveiller la première exécution : si l'image du runner n'a pas `git`, `actions/checkout` bascule sur le tarball et l'assertion échouera volontairement. NB 2026-08-17 : le runner est l'image officielle `ghcr.io/actions/actions-runner:2.335.1`, et l'étape `Assert the submodule is really checked out` fait échouer le job avec un message explicite plutôt que de passer vert à vide — le mode de défaillance est donc bruyant. Reste à confirmer sur un run réel.
- [ ] TEST: aucun check ne relie un CronJob à la version d'app qui expose l'endpoint qu'il appelle — le purge tapait une route absente de l'image depuis sa création, et rien n'a rien dit pendant des semaines. ⚠ 2026-08-17 : **hors de ce dépôt** — `grep -rl "kind: CronJob" charts/ kubernetes/` ne renvoie rien, il n'y a aucun CronJob ici. Le CronJob `portfolio/purge` et le Deployment dont il faudrait comparer la version vivent tous les deux dans le dépôt infra ; le check doit y être écrit.
- [ ] CHORE: migrer `infra/platform/security/values.yaml:51` de `kyvernoPolicies.validationFailureAction` vers `failureAction`, puis retirer l'alias déprécié du helper — l'alias n'existe que pour ce consommateur, et le test « the deprecated validationFailureAction value key still overrides » est là pour rendre sa suppression délibérée. ⚠ 2026-08-17 : **bloqué par ordonnancement inter-dépôts, et c'est voulu** — `charts/platform-security/templates/_helpers.tpl:41` dit noir sur blanc « Remove the alias only once infra sets `failureAction` ». Il n'y a pas de `infra/` ici : la première moitié (migrer le values infra) doit être faite dans l'autre dépôt et déployée AVANT que l'alias puisse tomber ici. Retirer l'alias maintenant casserait le consommateur en production.


## 🟢 Idées / backlog

## 🤖 Claude — recommandations

## ✅ Fait

- [x] 2026-08-19 — CHORE: câblage de `check-kyverno-crds.sh` sur un runner in-cluster — décision de routage tranchée (ARC, déjà déployé côté infra : controller `arc-systems`, scale sets `ci`) après vérification du dépôt infra, qui a invalidé deux de mes prémisses : UFW est **désactivé** sur les serveurs k3s depuis le 2026-08-17 (le pare-feu réel est Hetzner Cloud, en terraform), donc les options « ajouter à `k3s_api_extra_allow_from` » auraient été des no-op ; et la whitelist de l'AppProject `platform` autorise déjà `ClusterPolicy`, `ClusterRole` et `ClusterRoleBinding`. Côté k3s-lab : `runs-on: k3s-lab` + `.github/actionlint.yaml` (sans quoi actionlint rejette le label et casse `shell-ci`), suppression complète de la gestion du secret `KUBECONFIG`, installation de `kubectl` épinglée sur la version du cluster (v1.36.3) et vérifiée par sha256, **sans `sudo`** — l'image ARC tourne en utilisateur non privilégié avec `allowPrivilegeEscalation: false`, un `sudo mv` aurait marché sur `ubuntu-latest` et échoué précisément sur le runner visé. Le `ClusterRole` que j'avais ajouté à `kubernetes/base/rbac/` a été retiré : aucune Application ArgoCD ne déploie ce répertoire, il y était inerte — le manifeste complet (SA + ClusterRole + binding) est prêt pour `infra/platform/security/manifests/`
- [x] 2026-08-18 — TEST: `scripts/check-play-order.sh` + `tests/bats/play-order.bats` (10 tests) — assertion réelle sur l'ordre des plays, prise sur `ansible-playbook --list-tasks` (l'ordre qu'ansible résout lui-même, pas une lecture du YAML) : tout playbook qui pose des règles UFW doit se terminer par `ufw_enable`, exactement une fois. `--syntax-check` et `ansible-lint` passent tous les deux sur des plays dans le mauvais ordre, donc aucun des deux ne pouvait attraper ça. Vérifié sur 3 régressions réelles : `ufw_enable` avancé, un rôle ajouté après lui, et `ufw_enable` supprimé — trois messages distincts. Molecule écarté : demande Docker, indisponible ici
- [x] 2026-08-18 — CHORE: `scripts/check-ansible-collections.sh` + `tests/bats/ansible-collections.bats` (12 tests), câblé dans le job `ansible` **avant** ansible-lint — compare `ansible-galaxy collection list` aux pins de `requirements.yml`, en retenant la copie la plus en amont du chemin de recherche (celle qui tourne réellement). Sans lui, une collection absente ressort en `syntax-check[unknown-module]`, règle `unskippable`, sous le message trompeur « couldn't resolve module/action » qui envoie chercher une faute de frappe dans un fichier intact. Testé sur cette machine : détecte que l'ansible Homebrew masque les deux pins (13.3.0 et 2.2.2 devant 12.5.0 et 2.1.0)
