# TODO

## 🔴 En cours

## 🟡 À faire
- [ ] CHORE: appliquer le patch `.env.example` — **action manuelle obligatoire** : le fichier est refusé en écriture à l'agent (règle `.env*`, vérifié par `Edit` et par `touch`). Contenu exact fourni dans la session du 2026-08-06
- [ ] FIX(cluster): la StatefulSet `vault` déclare `hashicorp/vault:1.21.4` mais le pod `vault-0` tourne toujours en **1.21.2** — `updateStrategy: OnDelete`, `currentRevision != updateRevision`, `updatedReplicas` vide. L'upgrade ne partira jamais seul : il faut supprimer le pod, donc un unseal derrière. Vault porte des CRITICAL triés « upstream-blocked » — vérifier que 1.21.4 ne les corrige pas avant de planifier. **Bloqué** : `kubectl` est en lecture seule pour l'agent, et c'est une opération de production
- [ ] FIX: `portfolio/purge` échoue en **401** à chaque run (04:00 UTC) — seul reliquat réel du §10, le reste étant traité ou périmé. Le `WORKER_SECRET` de `portfolio-secrets` ne correspond pas à ce qu'attend la gateway ; `curl -f` ne réessaie pas un 4xx, donc le Job échoue franchement. Corrélé aux 12 restarts de `portfolio-7b77fcbfb4-ng6kj`. À traiter côté Vault/ESO (`secret/portfolio/prod`), pas côté CronJob. Ne pas se contenter de supprimer les Jobs en échec : ça masque le symptôme, le run suivant échoue pareil
- [ ] CHORE: commiter + pousser les 2 changements `infra` de l'étape 2 (working tree seulement pour l'instant) — ArgoCD synchronise depuis le remote, donc les 6 ClusterPolicy n'apparaîtront pas tant que ce n'est pas poussé. Corriger aussi le commentaire ligne 47 de `platform/security/values.yaml`, resté en « Stage 1 » juste au-dessus d'un `enabled: true`

## 🟢 Idées / backlog
- [ ] Kyverno 3.8 déprécie `spec.validationFailureAction` au profit de `spec.rules[].validate.failureAction` (visible dans le schéma des CRD rendues). Les 6 policies utilisent encore la forme dépréciée : ça marche, mais ça cassera à un major. `tests/bats/platform-security-render.bats` fige le contrat actuel et devra suivre
- [ ] Généraliser `check-rendered-images.sh` aux pins `image.tag` sans `repository:` (le cas Traefik, qui garde pour l'instant son script dédié à cause du repli sur l'appVersion du subchart)

## 🤖 Claude — recommandations
- [ ] CHORE: `main` local est en retard sur `origin` — `v0.18.4` (commit de release `aceff8c`) n'est pas un ancêtre de `HEAD` (`766e756`). Le travail `platform-security` encore en staging local est **déjà publié** dans 0.18.4 : seul le numéro de version diffère. Faire un `git pull` avant de committer pour éviter de re-livrer une version déjà sortie
- [ ] FIX: `portfolio-7b77fcbfb4-ng6kj` cumule 12 restarts (dernier il y a 29h) et la gateway répond 401 au purge — les deux pointent vers le même `WORKER_SECRET`. Diagnostiquer ensemble plutôt que séparément
- [ ] TEST: aucun check ne vérifie que les CRD Kyverno existent quand `platform-security.kyverno.enabled` est vrai — l'audit le suggérait (§1, « add a live check in the spirit of the existing Traefik jobs ») et ça reste le seul garde-fou manquant du rollout
- [ ] ~~CHORE: trancher sur `platform-deployment`~~ — **retiré le 2026-08-06**. Reposait sur le §4 de l'audit, lui-même retiré par son auteur : le chart est le point d'entrée public « Try it » du README, avec un rapport d'install k3d vérifié, et l'alignement des pins est automatisé par `release-charts.yml` depuis le 2026-08-03. Il n'y a pas de corvée, et supprimer le chart aurait cassé un artefact publié

## ✅ Fait
- [x] 2026-08-06 — FIX: §10 (partie historique des Jobs) — `failedJobsHistoryLimit: 3` → `1` sur `vault-auto-unseal` et suppression des 3 Jobs en échec de 29j. Le namespace `vault` ne contient plus que des Jobs `Complete`. Vérifié au passage que 2 des 3 reproches de l'audit étaient périmés : les limites d'historique étaient déjà posées partout, et la détection du seal status était déjà corrigée — par `2>&1`, pas par le code retour 0/2 que suggérait l'audit, lequel aurait **réintroduit** le bug (`vault status` renvoie rc=1 même déscellé sur ce nœud raft)
- [x] 2026-08-06 — CHORE: étape 2 du rollout `platform-security` appliquée dans `infra` — pin `0.18.3` → `0.18.4` sur la **seule** entrée platform-security (`argocd/applicationsets/platform.yaml:79`) et `kyvernoPolicies.enabled: true` (`platform/security/values.yaml:48`). 0.18.4 vérifiée publiée sur GHCR avant modification. Les deux fichiers ont un piège de sed multi-occurrences (4 pins `0.18.3` dans l'ApplicationSet, `trivy-operator.enabled: false` ligne 38 dans les values) — les commandes ont été ancrées sur le numéro de ligne et le résultat vérifié ligne à ligne. Reste à commiter/pousser
- [x] 2026-08-06 — TEST: `tests/bats/platform-security-render.bats` (9 tests) fige le contrat de rendu — 6 ClusterPolicies, `Audit` par défaut et jamais `Enforce`, override vers `Enforce` fonctionnel, policies gatées sur le moteur, trivy-operator off (assertion rendue falsifiable par un test « le gate est réel »)
- [x] 2026-08-06 — FIX: §8 — pin vault 1.21.2 → 1.21.4 + nouveau `scripts/check-rendered-images.sh` (généralisation offline de `check-traefik-image.sh`) qui échoue si un pin `repository:`/`tag:` n'atteint pas le rendu. 11 tests bats, job CI `rendered-images`. Le commentaire de `values.yaml` documente que `ha.enabled: false` ne décrit aucun cluster réel (le vrai Vault tourne en raft)
- [x] 2026-08-06 — CHORE: `.github/dependabot.yml` — sans lui les SHA épinglés la veille étaient immuables donc silencieusement périmés. Groupe unique, `chore` scope, hebdomadaire
- [x] 2026-08-06 — DOCS: §9 — `docs/configuration.md` ne prétend plus que les versions sont gérées par un `renovate.json` inexistant ; les 5 variables de version mortes sont documentées comme telles et pointent vers `charts/*/Chart.yaml` ; `ARGOCD_VERSION` (seule réellement lue, par `taskfiles/argocd.yml`) documentée et alignée sur 9.5.17 ; `K3S_VERSION` corrigé en v1.32.13+k3s1 dans les docs
- [x] 2026-08-06 — CHORE: §11 — `kevin@example.com` retiré de `charts/platform-deployment/Chart.yaml`, remplacé par `url:` vers le profil GitHub, cohérent avec le choix délibéré de `SECURITY.md` de ne pas publier d'adresse
- [x] 2026-08-06 — FIX: `k3s_installer_checksum` renseigné dans les rôles k3s_server et k3s_agent (sha256 de get.k3s.io vérifié stable sur deux téléchargements) — plus de script exécuté en root sans vérification
- [x] 2026-08-06 — FIX: `permissions: contents: read` au niveau workflow dans `ci.yml` — vérifié que les 2 jobs consommant `GITHUB_TOKEN` fonctionnent sans token du tout
- [x] 2026-08-06 — FIX: les 6 reusable workflows et les 4 actions tierces épinglés sur SHA 40 caractères — SHA résolus = exactement ce que `@main`/`@v5` pointaient déjà, donc aucun changement de comportement
- [x] 2026-08-05 — FIX(URGENT): 6 pins infra réalignés sur 0.18.3 (rendu revalidé chart par chart ; ArgoCD 0.9.2→0.18.3 ne change pas la version d'ArgoCD, argo-cd reste 9.5.17/v3.4.3) et `traefik-advisories` échoue désormais si `INFRA_READ_TOKEN` est absent
- [x] 2026-08-05 — FIX(URGENT): seuil CVE bloquant — alerte `TrivyUnacceptedCriticalCVEs` (severity critical) sur les 17 CRITICAL d'images non triées, invisibles des 2 règles existantes ; description de baseline périmée corrigée ; §2 de l'audit corrigé (chiffres non dédupliqués)
- [x] 2026-08-05 — FIX(URGENT): déployer `platform-security` — policies en `Audit` configurable, trivy-operator désactivé (doublon amont), entrée ajoutée à l'ApplicationSet infra en rollout 2 étapes
- [x] 2026-08-05 — FEAT: full audit of the repo, track security vulnerabilities, verify everything is working, make recommandations
