# TODO

## 🔴 En cours

## 🟡 À faire

- [ ] TEST: le job CI `k3s-lab-pin` n'a jamais tourné (ajouté depuis un sandbox sans accès Actions) — surveiller la première exécution : si l'image du runner n'a pas `git`, `actions/checkout` bascule sur le tarball et l'assertion échouera volontairement. NB 2026-08-17 : le runner est l'image officielle `ghcr.io/actions/actions-runner:2.335.1`, et l'étape `Assert the submodule is really checked out` fait échouer le job avec un message explicite plutôt que de passer vert à vide — le mode de défaillance est donc bruyant. Reste à confirmer sur un run réel.
- [ ] CHORE(URGENT): donner à la CI un accès lecture seule au cluster, puis décommenter le `schedule:` de `.github/workflows/kyverno-crds.yml` — **c'est la moitié manquante du câblage de `check-kyverno-crds.sh`** (2026-08-17). Le workflow existe et tourne en `workflow_dispatch`, mais rien ne le déclenche tout seul, donc le trou d'origine (« il ne verra la prochaine panne que si quelqu'un pense à le lancer ») n'est refermé qu'à moitié. Deux prérequis, tous deux des décisions : 1) un secret `KUBECONFIG` pour un principal **lecture seule** (`get`/`list` sur `crd` et `clusterpolicy` suffisent — surtout pas un kubeconfig cluster-admin) ; 2) une route vers l'API 6443, que les rôles ansible ferment à `k3s_ufw_allow_from` (10.0.0.0/16) + `k3s_api_extra_allow_from` (vide par défaut) : un runner GitHub-hosted a une IP publique dynamique et est donc bloqué. Ouvrir 6443 aux plages GitHub élargit nettement l'exposition ; un runner self-hosted dans le cluster ou sur le subnet WireGuard garde l'API privée et est le meilleur compromis. Tant que ce n'est pas fait, activer le `schedule` ferait échouer le job chaque matin **et ouvrirait une issue de suivi** pour une cause qui n'a rien à voir avec Kyverno.
- [ ] TEST: aucun check ne relie un CronJob à la version d'app qui expose l'endpoint qu'il appelle — le purge tapait une route absente de l'image depuis sa création, et rien n'a rien dit pendant des semaines. ⚠ 2026-08-17 : **hors de ce dépôt** — `grep -rl "kind: CronJob" charts/ kubernetes/` ne renvoie rien, il n'y a aucun CronJob ici. Le CronJob `portfolio/purge` et le Deployment dont il faudrait comparer la version vivent tous les deux dans le dépôt infra ; le check doit y être écrit.
- [ ] CHORE: migrer `infra/platform/security/values.yaml:51` de `kyvernoPolicies.validationFailureAction` vers `failureAction`, puis retirer l'alias déprécié du helper — l'alias n'existe que pour ce consommateur, et le test « the deprecated validationFailureAction value key still overrides » est là pour rendre sa suppression délibérée. ⚠ 2026-08-17 : **bloqué par ordonnancement inter-dépôts, et c'est voulu** — `charts/platform-security/templates/_helpers.tpl:41` dit noir sur blanc « Remove the alias only once infra sets `failureAction` ». Il n'y a pas de `infra/` ici : la première moitié (migrer le values infra) doit être faite dans l'autre dépôt et déployée AVANT que l'alias puisse tomber ici. Retirer l'alias maintenant casserait le consommateur en production.

## 🟢 Idées / backlog

## 🤖 Claude — recommandations
- [ ] CHORE: épingler `community.general` et `ansible.posix` dans `ansible/requirements.yml` est fait, mais rien ne vérifie que les versions installées correspondent — un `ansible-galaxy collection list` divergent passerait inaperçu, et les règles `syntax-check` d'ansible-lint sont `unskippable` donc une collection manquante fait échouer la CI sous un message trompeur (« couldn't resolve module/action »)
- [ ] TEST: aucun test ne couvre les rôles ansible au-delà du lint — `molecule` ou un simple `--check` contre un conteneur donnerait une vraie assertion sur l'ordre des plays, notamment que `ufw_enable` reste bien le dernier
- [ ] FIX: `GITHUB_TOKEN` et `UPSTASH_REDIS_REST_TOKEN` valent littéralement `placeholder` dans `secret/portfolio/prod` (même sha256, 11 octets) — repéré en diagnostiquant le purge ; le rate-limit Upstash du middleware est donc silencieusement inopérant

## ✅ Fait

- [x] 2026-08-18 — CHORE: corriger l'affirmation périmée sur les bumps d'actions dans l'en-tête de `ci.yml` — la recommandation d'origine (« aucun updater automatique ») était **fausse** : `.github/dependabot.yml` existe depuis le 2026-08-06, couvre l'écosystème `github-actions` en `directory: /` (donc aussi le nouveau `kyverno-crds.yml`), groupe tout en une PR, et a déjà produit `2161d07` et `285115b`. Le défaut réel était le commentaire de `ci.yml`, qui affirmait le contraire et accusait `docs/configuration.md` d'avoir tort alors que la doc était juste
- [x] 2026-08-18 — FIX: qualifier les handlers `Reload UFW` par rôle (`Reload UFW (k3s_server)` / `(k3s_agent)` / `(wireguard)`, 6 `notify:` mis à jour) — les trois rôles définissaient le même nom, et `k3s_server` + `wireguard` s'appliquent dans le MÊME play : une définition en masquait une autre, sans conséquence uniquement parce que les corps étaient identiques
- [x] 2026-08-18 — CHORE: passer `ansible-lint` en bloquant dans `ci.yml` (+ `working-directory: ansible`, version épinglée `26.8.0`) et ajouter `.ansible-lint`. Dette réellement mesurée cette fois : 40 × `var-naming[no-role-prefix]`, toutes des instances du partage de variables **voulu** (`k3s_version` entre server/agent, `k3s_lab_ufw_*` entre les 5 rôles) → règle désactivée avec justification ; 8 `syntax-check[*]` étaient des artefacts d'environnement (collections absentes, `roles_path` non résolu hors de `ansible/`). Restait **1 vrai défaut**, corrigé : `risky-shell-pipe` sur « Derive server public key » (`wireguard`), où un échec de `wg pubkey` derrière le pipe passait par le code de retour d'`echo` et laissait une clé publique vide filer dans `wg0.conf`. Arbre désormais `production`, 0 failure
- [x] 2026-08-17 — DOCS: documenter `k3s_lab_ufw_enabled` — nouvelle section « Ansible role variables (not `.env`) » dans `docs/configuration.md` (les 3 variables, pourquoi les 5 rôles les redéclarent, le repli `common_ufw_enabled`, et pourquoi `ufw_enable` doit rester le dernier play) + renvoi dans `.env.example`, qui précise que ce **n'est pas** une variable `.env` — c'était le piège de départ
- [x] 2026-08-17 — CHORE: supprimer le handler mort `Restart UFW` (`ansible/roles/common/handlers/main.yml`, fichier et dossier retirés) — notifié par aucune tâche ; le notifier aurait ajouté un `ufw reload` sans effet (ufw applique ses règles immédiatement, et un reload sur pare-feu inactif est un no-op) sous un nom qui contredisait son corps `state: reloaded`
- [x] 2026-08-17 — TEST: job CI `ansible` dans `ci.yml` — `ansible-playbook --syntax-check` sur les 4 playbooks (bloquant, ajouté aux `needs` de `docs`) + `ansible-lint` en **advisory** (`continue-on-error`) faute de pouvoir mesurer sa dette existante hors ligne. Vérifié non vacueux : un rôle mal orthographié (`ufw_enabel`) fait bien échouer la boucle
- [x] 2026-08-17 — CHORE: câbler `check-kyverno-crds.sh` — nouveau workflow `.github/workflows/kyverno-crds.yml` (render + CRD servi + policies réellement présentes, résumé de job, issue de suivi ouverte/mise à jour/fermée comme `traefik-advisories.yml`). `workflow_dispatch` actif ; le `schedule:` quotidien reste **commenté** tant que la CI n'a pas d'accès cluster — voir la tâche `CHORE(URGENT)` ci-dessus, qui est la moitié restante
- [x] 2026-08-17 — CHORE: porter 5 correctifs dans `k3s-lab` — 1) `bash /tmp/k3s-install.sh` dans `k3s_server` **et** `k3s_agent` (`/tmp` en `noexec`, CIS) ; 2) UFW activé en DERNIER via un rôle `ufw_enable` dédié, appliqué en play final de `site.yml`, `k3s-server.yml` et `k3s-agent.yml` ; 3) `register`/`until`/`retries` sur les 8 tâches UFW ; 4) interrupteur unique `k3s_lab_ufw_enabled` (repli sur l'ancien `common_ufw_enabled`) redéclaré dans les 5 rôles, car les `defaults/` ne sont en portée que pendant leur propre rôle ; 5) `log_martians` mis à `0` (plutôt que retiré du dict) pour désactiver aussi les nœuds déjà provisionnés
