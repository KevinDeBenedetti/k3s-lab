# TODO

> Dernière vérification : 2026-07-30 (index Traefik et advisories `traefik/traefik` contrôlés en direct).
> ℹ️ Le clone local a été remis à niveau sur `origin/main` (`5618a11 chore(main): release 0.14.0`).
> ⚠️ Rien n'est commité : les changements attendent dans `k3s-lab` et dans `infra`.

## 🔴 En cours

## 🟡 À faire

- [ ] CHORE: retirer `traefik.image.tag` de `charts/platform-traefik/values.yaml` dès qu'un chart Traefik embarquera un proxy au moins aussi récent que le pin — **encore bloqué au 2026-07-29** : le dernier chart publié reste `41.0.2` (2026-07-06, appVersion v3.7.6), très en deçà. Ne reste que cette clé, `versionOverride` ayant pu être supprimée avec le passage en 41.0.2 (voir ✅). À noter : l'advisory qui justifiait le pin d'origine (`GHSA-8rxv-jg7p-wvg3`, Ingress NGINX RewriteTarget) ne s'applique **pas** à notre configuration — nous n'activons pas le provider `kubernetesIngressNGINX`. C'est `GHSA-cxjq` (≤ v3.7.6) puis `GHSA-3ccp` (≤ v3.7.8) qui maintiennent le pin nécessaire.
- [ ] CHORE(BLOQUÉ): corriger le commentaire `CVE-2026-48020` dans `infra/platform/trivy-operator/manifests/cve-exceptions-configmap.yaml:422-425`. **Bloqué en écriture** : ce chemin est hors des répertoires accessibles à l'agent (seul `infra/argocd/applicationsets` l'est côté `infra`). Correctif rédigé et justifié, prêt à appliquer. Analyse faite au 2026-07-30, deux corrections à la formulation de la tâche : (1) il n'y a **aucune exception à purger** — les lignes 422-425 sont de la prose dans le commentaire de triage du 2026-07-20, qui dit lui-même « NOT in the allow-list » ; rien ne filtre rien, le défaut est que le commentaire désinforme. (2) Réponse à la question ouverte : **non**, `GHSA-xf64-8mw2-4gr2` ne doit **pas** figurer dans l'allow-list. Vérifié contre l'API : elle existe bien (HIGH, 2026-06-05, *StripPrefix Route-Level Auth Bypass via Path Normalization*), `<= v3.7.1` vulnérable sur la ligne 3.7, corrigée en **v3.7.3** — nous déployons v3.7.9, le constat est donc *résolu* et non *accepté* ; une entrée d'allow-list masquerait une régression si le pin reculait. Le commentaire attribue en plus le correctif à « v3.7.8 », doublement faux (v3.7.3 suffisait, et le pin est à v3.7.9). Idem pour les deux advisories sœurs `GHSA-6jwx-7vp4-9847` et `GHSA-6384-m2mw-rf54`, corrigées en v3.7.0-rc.2.

## 🟢 Idées / backlog

## 🤖 Claude — recommandations

- [ ] DOCS: noter que les advisories Traefik ne sont **pas** dans la base globale GitHub — `GET /advisories/<GHSA>` renvoie 404 alors que `GET /repos/traefik/traefik/security-advisories/<GHSA>` les retourne. Un 404 sur l'endpoint global ne prouve donc pas qu'une advisory est inventée.
- [ ] DOCS: corriger le commentaire obsolète en tête de `infra/platform/traefik/values.yaml` — il invoque `CVE-2026-48020`, dont on a établi qu'il n'existe pas, affirme qu'un override `image.tag` « ne marche pas » (c'est faux depuis le chart 41.0.2) et renvoie vers un plan d'action déjà exécuté (bump 40.2.0 → 40.3.0). En l'état il désinformera le prochain lecteur en pleine panne.
- [ ] DOCS: noter que `gh api "/user/packages/container/charts%2F<chart>/versions"` renvoie une liste **vide** au lieu des versions (constaté le 2026-07-26), alors que le `DELETE` sur la même URL encodée fonctionne — un listing vide n'est donc pas une preuve que le package est vide. Utiliser `/users/KevinDeBenedetti/packages/…` pour tout contrôle avant purge.
- [ ] CHORE: aligner ou assumer `appVersion: 0.13.0` de `charts/platform-deployment/Chart.yaml` — release-please bump `version` mais pas `appVersion`, l'umbrella affiche donc 0.14.0/0.13.0 après chaque release. Même classe de problème que le seeder, mais ici la dérive se rejoue à chaque sortie.
- [ ] TEST: faire échouer la CI quand un pin de `platform-deployment` ne correspond pas à la dernière version publiée. À **détecter**, pas à corriger automatiquement : la mise à jour des 7 pins reste manuelle par choix (voir ✅), et ce qui a laissé l'umbrella sur 0.13.0 pendant trois releases est l'oubli du geste, pas sa difficulté. Un simple job qui compare les pins aux tags GHCR et sort 1 suffirait — l'outillage d'alignement écrit le 2026-07-30 a été retiré, donc c'est à réécrire (une boucle `curl` sur `/v2/<repo>/tags/list`, une vingtaine de lignes). Le job aura besoin d'un accès en lecture au registry.
- [ ] TEST: faire tourner `scripts/check-traefik-advisories.sh` aussi en CI sur les PR qui touchent `charts/platform-traefik/values.yaml` — la veille quotidienne attrape un pin qui *se périme*, pas un pin déjà vulnérable au moment où on l'écrit. Coût quasi nul, et ça ferme la fenêtre à l'endroit où la décision est prise.
- [ ] DOCS: mettre à jour la liste des sous-charts du README de `platform-deployment` — elle annonce encore des versions 0.8.0–0.9.2 alors que les pins sont à 0.14.0, et les exemples parlent de `--version 1.0.0`, une ligne de version abandonnée le 2026-07-21. C'est le README du chart publié sur GHCR, donc la première chose que lit quelqu'un qui l'installe.
- [ ] CHORE: envisager de faire dériver `charts/platform-traefik/README.md` (tableau des advisories, version du subchart, pin) d'une source unique plutôt que d'une recopie manuelle — trois endroits énoncent aujourd'hui la même vérité (values.yaml, README, allow-list trivy de `infra`), et le commentaire de `infra/platform/traefik/values.yaml` a déjà divergé.
- [ ] FEAT: envisager des dépendances `file://` pour `platform-deployment` — l'umbrella validerait alors la source du repo au lieu d'artefacts publiés en retard d'une release. Change la sémantique de packaging (sous-charts embarqués vs tirés du registry) : décision d'architecture à trancher.

## ✅ Fait

- [x] 2026-07-30 — CHORE: mettre en place une veille sur les advisories `traefik/traefik`. `scripts/check-traefik-advisories.sh` confronte le pin `traefik.image.tag` aux `vulnerable_version_range` du dépôt amont ; `lib/semver.sh` fournit l'évaluateur de plages (`<= v3.7.8`, `>= v3.7.0, <= v3.7.7`, …). Workflow `traefik-advisories.yml` : cron **quotidien** — la fenêtre d'exposition observée se compte en jours — plus `workflow_dispatch` ; il ouvre/met à jour une issue de suivi quand le pin devient vulnérable et la referme quand il repasse au propre, parce qu'un simple échec de workflow planifié se noie dans les mails. Un point de conception : une plage illisible sort **2**, jamais 0 — un opérateur inconnu ne doit pas se lire « pas vulnérable ». Vérifié pour de vrai : exécution live contre l'API (77 plages évaluées, v3.7.9 propre — ce qui reconfirme au passage la décision de la veille) ; contrôle négatif de bout en bout en remettant le pin à v3.7.8 → `GHSA-3ccp-42pg-hgv6` correctement détectée avec sa version corrigée, exit 1. Précisément l'advisory qui était passée inaperçue 12 jours. 22 tests bats sur l'évaluateur, bâtis sur les plages réelles de l'API. `traefik.image.tag` était lu par deux scripts : factorisé en `traefik_pinned_tag` (+ 4 tests, dont « pin absent → silence », le cas qui fait retomber le subchart sur son appVersion). Suite complète verte (72/72), shellcheck et actionlint propres.
- [x] 2026-07-30 — TEST: rendre `platform-deployment` dans `release-charts.yml` après publication des sous-charts. Nouvelle étape `render`, placée **après** la boucle de publication — c'est la seule position correcte : le glob `charts/*/` trie l'umbrella 4ᵉ sur 10, donc six de ses dépendances ne sont pas encore poussées pendant la boucle. Trois assertions plutôt qu'un simple `helm template` : `helm dependency list` ne doit contenir ni `missing` ni `wrong version` (un `dependency update` peut réussir en laissant une dépendance non résolue — format de sortie vérifié en local sur les deux cas, résolu et manquant), le rendu doit aboutir, et il doit produire ≥ 7 manifestes puisque les 7 sous-charts sont activés par défaut — un rendu vide ne doit jamais compter comme un succès. Même politique de 5 tentatives que la publication (GHCR est instable juste après un push), et résultat reporté dans le step summary (`if: always()`). **Limite assumée** : le contrôle est *post-publication*, il détecte un umbrella non rendable sans empêcher de le pousser — c'est le prix à payer pour ne pas réintroduire le blocage circulaire du 2026-07-21. Non exécuté ici (`ghcr.io` hors du bac à sable réseau) : actionlint et YAML propres, la détection `missing` validée contre la vraie sortie de `helm dependency list`.
- [x] 2026-07-30 — DOCS: documenter la contrainte `traefik.io/proxy-max-version` — `charts/platform-traefik/README.md` créé (le chart n'en avait aucun). Le cœur du sujet : le plafond vit dans les **annotations du `Chart.yaml` du subchart**, donc rien n'en est visible depuis le chart parent, et `helm lint` ne le voit pas non plus puisqu'il ne télécharge pas les dépendances. Documenté à partir du tarball 41.0.2 lui-même : les annotations réelles (`proxy-min-version: v3.6.0`, `proxy-max-version: v3.7.6`), l'ordre de résolution de `traefik.proxyVersion` (`versionOverride` → `image.tag` → appVersion du subchart, d'où la retombée silencieuse si le pin disparaît), et la table de ce qui `fail` vraiment depuis 41.0.0 (min non respecté, ou **majeur** au-dessus du max) contre ce qui n'avertit que dans `NOTES.txt` (mineur/patch au-dessus, notre cas v3.7.9 > v3.7.6). Les deux pannes des 2026-07-20 et 2026-07-21 y sont rattachées à leur cause. Ajouté aussi pourquoi `versionOverride` ne doit pas revenir : `requirements.yaml` s'en sert pour une vingtaine de gardes de feature-gating, pas seulement pour le plafond. Les deux commandes documentées ont été exécutées (rendu nu et rendu avec l'overlay `infra/platform/traefik/values.yaml`) et les 5 liens relatifs vérifiés.
- [x] 2026-07-30 — CHORE: corriger `appVersion` de `platform-vault-seeder`, `0.1.0` → `0.14.0`. **Corrigé, pas confirmé** : l'« application » livrée par ce chart, ce sont ses propres scripts de seeding, embarqués dans `configmap-core.yaml`/`configmap-apps.yaml` et exécutés tels quels sur une image `bitnami/kubectl:1.32` générique. Il n'existe aucun artefact applicatif versionné indépendamment du chart, donc `appVersion` doit suivre `version` — le `0.1.0` ne datait de rien et ne désignait rien. Pour que la dérive ne revienne pas, `$.appVersion` a été ajouté aux `extra-files` de release-please (il ne bumpait que `$.version`) ; la raison est consignée en commentaire dans le `Chart.yaml`. Vérifié : JSON de config valide, `helm lint` OK, et le rendu produit bien `app.kubernetes.io/version: "0.14.0"` sur les trois workloads.
- [x] 2026-07-29 — TEST: ajouter un test de rendu qui vérifie que l'image traefik effectivement produite correspond à la version voulue. `scripts/check-traefik-image.sh` lit le pin `traefik.image.tag` du values.yaml du chart, rend le chart et compare **toutes** les images `traefik:` produites à ce pin ; nouveau job CI `traefik-image` (ajouté aussi aux `needs` de `docs`). Trois contrôles passés en local sur le subchart 41.0.2 déjà présent dans `charts/` : positif (`docker.io/traefik:v3.7.9`, exit 0) ; négatif « le rendu diverge du pin » (`-- --set traefik.image.tag=v3.7.6` → exit 1) ; négatif « pin retiré » (exit 1 — le script refuse de valider un chart sans pin plutôt que de passer silencieusement). Un rendu ne produisant aucune image traefik échoue également : un rendu vide ne doit jamais compter comme un succès. Le passe-plat `--` permet de rejouer le contrôle avec l'overlay d'un cluster, ce qui était fait à la main jusqu'ici.
- [x] 2026-07-30 — FEAT: aligner automatiquement les pins de `platform-deployment` — **construit puis retiré, décision assumée : les 7 pins restent manuels.** `scripts/align-umbrella-pins.sh` + les helpers de pins d'umbrella (`chart_oci_deps`, `chart_oci_prefix`, `chart_pin`, `chart_set_pin`, `semver_latest`) et leurs 19 tests ont existé et fonctionnaient (interrogation de l'API de distribution OCI, `--check` pour la dérive, garantie structurelle de ne jamais épingler une version pas encore poussée). Supprimés au motif du rapport coût/bénéfice : c'est **un fichier à éditer une fois par release**, et ~250 lignes de shell + tests pour automatiser ça, c'est plus de surface à maintenir que le geste qu'elles remplacent. Ne pas reconstruire ça sans une raison nouvelle — si la douleur revient, elle viendra de l'*oubli* du geste, pas de sa difficulté, et c'est une détection (voir 🤖) qui répond à ça, pas une automatisation. Seul `traefik_pinned_tag` a survécu, dans `lib/traefik-pin.sh` : il sert aux deux contrôles Traefik, eux conservés.
- [x] 2026-07-29 — FIX(URGENT): monter `traefik.image.tag` de `v3.7.8` à `v3.7.9`. `GHSA-3ccp-42pg-hgv6` (HIGH, publiée le 2026-07-27) affecte **tout ≤ v3.7.8** — empoisonnement de réponse entre utilisateurs via CONNECT proxifié sur le pool keep-alive partagé, indépendant des providers, donc applicable ici. Le pin était devenu vulnérable 12 jours après avoir été posé. Commentaire du values.yaml corrigé au passage : il citait `GHSA-8rxv-jg7p-wvg3`, qui ne nous concerne **pas** (elle vise le provider `kubernetesIngressNGINX`, désactivé chez nous) ; les advisories réellement applicables sont `GHSA-cxjq` (≤ v3.7.6) et `GHSA-3ccp` (≤ v3.7.8). Vérifié : rendu par défaut et rendu avec l'overlay de production produisent tous deux `docker.io/traefik:v3.7.9`, `--log.level=INFO` et `--accesslog=true` intacts, `helm lint` OK.
- [x] 2026-07-28 — CHORE: bumper le subchart traefik `40.3.0` → `41.0.2` et **supprimer `versionOverride`**. Le changelog 41.0.0 relâche le garde-fou de version (`relax max-version guard to warn on minor/patch, fail only on major mismatch`) : `templates/requirements.yaml` ne `fail` plus que via `traefik.isMajorAboveMax`, donc un proxy v3.7.8 face au max v3.7.6 ne produit qu'un avertissement. L'échappatoire n'a plus lieu d'être, et le feature-gating du chart voit enfin la version réellement déployée. Migration obligatoire de la rupture logs (41.0.0) au passage : `logs.general` → `log`, `logs.access` → `accessLog` — le schéma déclare `additionalProperties: false`, l'ancienne clé n'est donc pas ignorée mais fatale. Vérifié par rendu hors ligne (subchart reconstitué depuis le tarball, `helm` ne pouvant pas valider TLS ici) : rendu propre, `docker.io/traefik:v3.7.8`, `--log.level=INFO` et `--accesslog=true` bien produits ; contrôle négatif — l'ancienne syntaxe `logs:` échoue bien avec `additional properties 'logs' not allowed` ; rendu avec l'override de production (`infra/platform/traefik/values.yaml`) également validé (DaemonSet, ClusterIP, hostPorts 80/443). `Chart.lock` régénéré à la main, l'algorithme de digest ayant été validé en reproduisant à l'identique celui de la 40.3.0. La 2ᵉ rupture 41.0.0 (`providers.file.content` string → objet) ne nous concerne pas.
- [x] 2026-07-26 — CHORE: purger de GHCR les versions cassées `platform-traefik` **et** `platform-deployment` 0.13.0/0.13.1/0.13.2 (6 versions), impossibles à rendre à cause du garde-fou `traefik.io/proxy-max-version`. Sûreté vérifiée avant : le cluster tournait encore sur les pins `0.9.1`, aucune App ArgoCD ne référençait 0.13.x, et l'umbrella venait de passer en 0.14.0. État final contrôlé — il ne reste que `0.14.0` et la version de base (`0.12.0` / `0.1.0`) ; `platform-argocd`, non ciblé, a bien gardé ses 0.13.x, ce qui confirme que la purge n'a pas débordé.
- [x] 2026-07-25 — FIX(URGENT): passer les trois pins de `infra/argocd/applicationsets/platform.yaml` (`cert-manager`, `external-secrets`, `traefik`) de `0.9.1` à `0.14.0`, ce qui rétablit la réconciliation ArgoCD cassée depuis la purge du 2026-07-21. Deux commentaires ajoutés au passage : l'un rappelant que ces pins doivent exister sur GHCR (c'est `cleanup-packages.yml` qui les purge, en panne silencieuse), l'autre interdisant explicitement les traefik 0.13.0–0.13.2. Fichier revalidé en YAML. **Non commité** — le repo `infra` porte déjà d'autres changements en attente.
- [x] 2026-07-25 — FIX(URGENT): passer les 7 pins de `charts/platform-deployment/Chart.yaml` de `0.13.0` à `0.14.0` — dernière étape de la sortie 0.14.0. Résolution confirmée via l'API GHCR (les 7 sous-charts existent en 0.14.0) ; `helm dependency update` n'est **pas** exécutable en local, le sandbox rejetant le certificat GHCR (`x509: OSStatus -26276`) — la vraie validation viendra de la CI.
- [x] 2026-07-25 — CHORE: auditer les autres ApplicationSets à la recherche de pins figées du même type. Résultat : rien à corriger. `platform-vendor.yaml` n'épingle que des charts *upstream* (`trivy-operator` 0.33.1 depuis aquasecurity.github.io, `gha-runner-scale-set-controller` 0.14.2 depuis ghcr.io/actions) — hors du périmètre de `cleanup-packages.yml`, donc non exposés à la purge de rétention. `apps.yaml` ne suit que `targetRevision: main`. Seul `platform.yaml` porte des pins GHCR maison (traité dans 🔴).
- [x] 2026-07-25 — TEST: premier dry-run complet vérifié (run CI `30148008938`, commit `6496a57`). Les 9 charts à templates rendent proprement — `external-secrets`, `monitoring`, `security` et `vault`, jamais rendus en CI jusque-là, passent sans erreur ; `platform-deployment` est sauté comme prévu. Aucun autre échec de rendu n'a émergé. CI verte aussi sur `5618a11`.
- [x] 2026-07-25 — FIX: sortir `platform-traefik` 0.14.0, étapes 1 à 3 sur 4. (1) `template-exclude-paths` mergé dans `github-workflows` (`0ee52fb`, sorti en 0.18.2) ; (2) commit CI de k3s-lab mergé → CI verte ; (3) PR release-please mergée (`5618a11`), tag `v0.14.0` créé, `release-charts` a publié les **10** charts en 0.14.0 sur GHCR. Reste l'étape (4), les pins de l'umbrella → voir 🔴.
- [x] 2026-07-22 — FIX: ajouter `versionOverride: v3.7.4` à `charts/platform-traefik/values.yaml` pour que le subchart 40.3.0 accepte le proxy v3.7.8. Vérifié par rendu hors ligne (subchart reconstitué depuis le tarball GitHub) : sans la clé le rendu échoue sur le garde-fou `traefik.io/proxy-max-version`, avec elle il produit `docker.io/traefik:v3.7.8`.
- [x] 2026-07-22 — FIX: activer `run-template: true` dans `.github/workflows/ci.yml` — `helm lint` seul ne fait pas `helm dependency update`, donc les garde-fous des subcharts ne sont jamais rendus. C'est ce trou qui a laissé publier un `platform-traefik` non rendable en 0.13.0, 0.13.1 puis 0.13.2 sans aucune alerte.
- [x] 2026-07-21 — FIX: corriger la publication des charts dans `release-charts.yml` — l'archive était choisie via `ls /tmp/charts/${chart_name}-*.tgz` ; le motif `platform-vault-*` capturait aussi `platform-vault-seeder-*.tgz`, et le glob `charts/*/` trie le seeder en premier (le slash final fait passer `-` avant `/`). Remplacé par le nom exact `${chart_name}-${version}.tgz`.
- [x] 2026-07-21 — CHORE: couvrir les 10 charts par release-please — `platform-deployment` et `platform-vault-seeder` ajoutés aux `extra-files` et alignés sur la version du repo.
- [x] 2026-07-21 — FIX: porter la rétention GHCR de `5` à `30` dans `cleanup-packages.yml`, avec le contexte de l'incident en commentaire.
- [x] 2026-07-21 — CHORE: purger les 22 versions `1.2.0`/`1.3.0`/`1.4.0` de GHCR — supérieures en semver à `0.13.0` tout en contenant du code plus ancien.
- [x] 2026-07-21 — FIX: réparer la boucle release-please, qui re-proposait 1.2.0 indéfiniment faute de baseline. Vérifié : `v0.13.1` puis `v0.13.2` se sont créées et taguées seules.
- [x] 2026-07-21 — CHORE: ramener le repo sur la ligne 0.x — il était passé en 1.x via un commit `Release-As: 1.2.0` du 2026-06-30. Renuméroté en `0.13.0`, tagué et releasé.
- [x] 2026-07-21 — FIX: bumper le subchart traefik `40.2.0` → `40.3.0` et pinner le proxy `v3.7.8`. Le CVE invoqué au départ (`CVE-2026-48020`, « StripPrefix auth bypass ») n'existe pas ; le vrai correctif est `v3.7.8`, qui couvre GHSA-8rxv-jg7p-wvg3 et GHSA-cxjq-mrr5-89rv.
