# TODO

## 🔴 En cours

## 🟡 À faire

- [ ] CHORE(URGENT): une fois la release 0.18.0 publiée, remonter les trois pins de `infra/argocd/applicationsets/platform.yaml` de `0.16.0` vers `0.18.0` — la production tourne sur les artefacts 0.16.0 : Traefik **v3.7.9**, couvert depuis le 2026-08-03 par `GHSA-fgjj-px3w-67xx` (HIGH, hijacking de backend cross-namespace via Gateway API) + 2 autres, et cert-manager **v1.20.2**, sous `GHSA-8rvj-mm4h-c258` (HIGH). Les deux correctifs partent dans 0.18.0. Vérifier les candidats avant de pinner : `scripts/check-deployed-charts.sh --only <chart> --chart-version 0.18.0`. La veille quotidienne ouvrira une issue tant que ce n'est pas fait — c'est voulu.

## 🟢 Idées / backlog

## 🤖 Claude — recommandations

- [ ] FIX: rendre l'umbrella installable avec `platform-security` activé — ses `ClusterPolicy` Kyverno sont validées par helm avant que les CRDs Kyverno de la même release n'existent (constaté le 2026-08-03 sur cluster vierge). Pistes : hook `post-install` sur les policies, ou CRDs Kyverno dans `crds/` du wrapper, ou sous-release séparée.
- [ ] FIX: rafraîchir le `NOTES.txt` de `platform-deployment` — il référence `--version 1.0.0` (ligne abandonnée le 2026-07-21) et `0.9.2`, et donne des instructions producteur (`helm dependency update charts/…`) inapplicables à un consommateur — même défaut que celui corrigé dans le README le 2026-08-01, resté dans les NOTES.




## ✅ Fait

- [x] 2026-08-03 — FIX(URGENT): re-poser le pin `traefik.image.tag: v3.7.10` — **trois advisories publiées le jour même** (divulgation coordonnée de la v3.7.10 du 2026-07-31) couvrent `v3.7.0 – v3.7.9` : `GHSA-fgjj-px3w-67xx` (HIGH, collision d'identité de routes Gateway API → hijacking de backend cross-namespace), `GHSA-62fc-8686-hfmq` (medium, bypass `allowCrossNamespace=false`), `GHSA-6765-c87h-8mrf` (low, spoofing BasicAuth). Détecté par le job CI `traefik-advisories` sur le push du soir — le système a fonctionné en conditions réelles, à J+0. Aucun chart amont ne livre v3.7.10 (41.1.1 reste en appVersion v3.7.9) : c'est le cas exact de l'escape hatch documenté — pin `image.tag` (jamais `versionOverride`), commentaire de `values.yaml` réécrit avec la consigne de le retirer au premier subchart ≥ v3.7.10, README mis à jour (table +3 lignes, mode « pinned » réactivé, historique complété). Dépassement du `proxy-max-version` (v3.7.9) d'un patch : warning NOTES seulement depuis 41.0.0. Au passage : `traefik_pinned_tag` strippe désormais les guillemets (`tag: "v3.7.10"` ≡ `tag: v3.7.10`, +1 test). Vérifié : scan advisories propre sur v3.7.10 (84 plages), rendu hors ligne produit bien `image: docker.io/traefik:v3.7.10`. 128 tests bats, 0 échec.
- [x] 2026-08-03 — FIX(URGENT): remonter cert-manager v1.20.2 → **v1.20.3** dans `charts/platform-cert-manager/Chart.yaml` — clôt `GHSA-8rvj-mm4h-c258` (HIGH) détecté le matin par la première exécution de `check-deployed-charts.sh` ; embarqué dans le même train de release que le fix Traefik ci-dessus (deux vulnérabilités de prod, un seul cycle release→pin). v1.20.3 vérifié : résolvable sur charts.jetstack.io et propre au scan (3 plages). Commentaire de traçabilité dans le `Chart.yaml`, README du chart aligné. ⚠️ `Chart.lock` et tgz vendorisé encore en v1.20.2 — `helm dependency update charts/platform-cert-manager` à lancer avant commit (TLS du sandbox bloqué).
- [x] 2026-08-03 — FIX: remonter les 7 pins de l'umbrella vers 0.17.0 (release publiée dans l'après-midi depuis le merge de la PR #156, antérieur à l'arrivée de l'automatisation — la PR automatique jouera à partir de la 0.18.0) — fait avec `scripts/bump-umbrella-pins.sh --version 0.17.0`, sa première utilisation réelle ; `check-umbrella-pins.sh` repasse 7/7.
- [x] 2026-08-03 — DOCS: requalifier le README de `platform-deployment` — l'encadré « published, but nothing currently deploys it » devenait trompeur une fois le chart promu porte d'entrée publique. Nouveau cadrage : « c'est le chemin *essayer*, pas la façon dont le cluster de l'auteur tourne » (les deux publics, explicites), enrichi du **rapport d'installation vérifiée du jour** : les deux caveats découverts (Traefik embarqué de k3s stock → flags `--disable` identiques à ceux des rôles Ansible ; `platform-security` ininstallable dans sa propre release → `--set platform-security.enabled=false`), le résultat mesuré (15/17 workloads prêts) et les deux retardataires expliqués comme dépendances, pas comme bugs. `--version 0.15.0` périmé corrigé au passage, et le « Try it » du README racine aligné (la commande donnée est celle qui marche *vraiment*, caveats en une ligne avec renvoi).
- [x] 2026-08-03 — CHORE: première installation réelle de l'umbrella `platform-deployment` — sur un k3d jetable (k3s v1.35.5, l'artefact 0.16.0 tiré de GHCR, digest vérifié). **Trois trouvailles.** (1) Sur un k3s *stock*, l'installation échoue immédiatement : k3s embarque son propre Traefik (39.0.7) et les CRDs `hub.traefik.io` entrent en conflit — or les rôles Ansible du repo désactivent `traefik`+`servicelb` au provisioning, donc l'umbrella n'a jamais visé un k3s stock alors que le README « Try it » disait « any Kubernetes cluster ». (2) Même sur cluster propre, les valeurs par défaut échouent : les `ClusterPolicy` Kyverno de `platform-security` sont validées par helm avant que les CRDs Kyverno de la même release n'existent — ArgoCD masque ce problème d'ordre par ses retries, helm seul non. (3) Le `NOTES.txt` est périmé (références `--version 1.0.0` — la ligne abandonnée — et `0.9.2`, et des instructions producteur du même défaut que celui corrigé dans le README le 2026-08-01). **Avec `--set platform-security.enabled=false` : `helm status: deployed`, 15/17 workloads pleinement prêts** (Traefik, ArgoCD complet, Prometheus, Loki, cert-manager, external-secrets) ; les deux retardataires sont des dépendances documentées du déploiement réel : vault attend `vault-tls` (+ init/unseal), grafana attend `grafana-admin-secret` (fourni par ESO+Vault). Vérifié par deux approches : attente pod par pod, puis état agrégé des workloads + verdict helm. Le README de l'umbrella documente désormais tout ça (tâche suivante).
- [x] 2026-08-03 — DOCS: `CONTRIBUTING.md` + `SECURITY.md` propres à `k3s-lab` — audit préalable des modèles du repo `.github` de profil (checkout local propre, `8b3a0a0`) : le `CONTRIBUTING.md` de profil est à jour et sert de socle (le fichier de `k3s-lab` y renvoie pour le flux général et ne porte que le spécifique : setup bats/shellcheck/prek, la règle « un lib sans son .bats est un signal d'alarme », l'interdit de toucher `version:`/`appVersion:`/`Release-As:` — territoire release-please — et les checks à lancer avant une PR touchant un composant sécurité) ; le `SECURITY.md` de profil, lui, **ne pouvait pas être hérité** : ses sections Supported/Scope parlent du repo `.github` lui-même et renvoient les projets consommateurs « to the respective repository » — circulaire vu depuis k3s-lab. Le `SECURITY.md` du repo dit : seule la dernière release 0.x est supportée (et pourquoi : la purge GHCR), advisory privée de préférence, mêmes délais de réponse que le profil, périmètre incluant « un chart d'ici pinne une version vulnérable » même si la faille est amont, et la veille quotidienne assumée publiquement.
- [x] 2026-08-03 — REFACTOR: le chemin de registre des checks « déployé » se lit désormais dans le `repoURL` de l'ApplicationSet — nouveau `applicationset_chart_repo` dans `lib/chart-deps.sh` (la source qui porte un `chart:` est celle du registre ; celle des values n'en a pas), 3 tests bats dont le piège « values source en premier » et le piège « `chart:` des éléments du générateur sans repoURL ». `check-deployed-charts.sh` : le fichier vivant prime, `--repo-base` explicite gagne, et l'absence de repoURL est un échec (« deviner un registre, c'est vérifier des charts que personne ne déploie »). Le défaut codé en dur ne survit que pour les modes sans ApplicationSet (`--only` + `--chart-version`). 127 tests bats (+3), vérifié sur le fichier vivant d'infra : `Registry from the ApplicationSet: ghcr.io/kevindebenedetti/charts`, mêmes verdicts.
- [x] 2026-08-03 — TEST: étendre la veille « déployé » aux autres charts de l'ApplicationSet — la crainte de la tâche (« la source d'advisories n'est pas forcément un dépôt GitHub ») ne s'est pas matérialisée : cert-manager et external-secrets publient tous deux sur leur repo GitHub, même API que Traefik. Nouveau `scripts/check-deployed-charts.sh` (table de composants : chart → subchart → repo d'advisories ; Traefik garde sa résolution pin-aware, les autres lisent l'`appVersion` du subchart embarqué) ; `check-deployed-traefik.sh` devient une façade de compatibilité (`--only platform-traefik`), son interface étant citée dans des issues et le README. Généralisations : `lib/advisories.sh` (ex-`traefik-advisories.sh`, supprimé) et `lib/subchart.sh` (extrait de `traefik-pin.sh`, qui délègue). Deux duretés du réel ont imposé de raffiner l'évaluateur : (1) `semver_in_range` lisait la virgule comme ET partout — faux pour les énumérations `=a,=b` et les plafonds par ligne `<a,<b,<c` publiés tels quels par cert-manager (un v1.15.3 aurait été déclaré sain face à `<v1.12.14,<v1.15.4,<v1.16.2`) ; désormais OR pour ces deux formes uniformes, ET conservé pour les intervalles ; (2) les plages amont sur-déclarent (ESO : `>0.1.0` vulnérable à jamais, `patched: 2.4.1`) — le scan consulte donc `patched_versions` : une version en plage est blanchie si sa propre ligne a un correctif atteint ou si elle est au-dessus de tous les correctifs, jamais si le correctif de sa ligne n'est pas atteint ; même logique sur plage imparsable (`master branch` + `patched: v0.10.2`). Câblé dans `traefik-advisories.yml` (section 2 couvre les 3 charts). **Première exécution réelle : v3.7.9 propre, ESO v2.5.0 propre (2 faux positifs neutralisés, motivés dans le log), et un vrai HIGH détecté — cert-manager v1.20.2 sous `GHSA-8rvj-mm4h-c258`, patché en 1.20.3.** 124 tests bats (+17 : advisories.bats nouveau, semver +3), CI lints propres.
- [x] 2026-08-03 — FEAT: automatiser le bump des pins de l'umbrella via PR — workflow post-release plutôt que Renovate (aucune config ni PR Renovate n'existe : la mention de l'ancien README était aspirationnelle), parce que le moment est structurellement le bon : juste après la publication, la contrainte « un pin ne référence qu'une version publiée » tient par construction. Nouveau `scripts/bump-umbrella-pins.sh` (l'écriture minimale : les `version:` du bloc `dependencies` et rien d'autre, vérifiée après coup par `chart_deps`, le même parseur que les checks — l'écrivain et le chien de garde ne peuvent pas diverger), 9 tests bats dont « ne touche ni `version:` ni `appVersion:` top-level » (territoire de release-please) et le diff byte-à-byte. Étape ajoutée à `release-charts.yml` : token de la GitHub App release (le `GITHUB_TOKEN` par défaut ne déclenche pas la CI sur ses PRs), branche `chore/umbrella-pins-<version>`, PR idempotente. Le commit est un **`chore:` délibérément non-releasing** — un `fix:` créerait une boucle infinie release → PR → merge → release ; avec `chore:` les pins alignés partent avec la release suivante, le retard d'une release déjà assumé. 107 tests bats (+9), `actionlint` propre. Première preuve réelle à la prochaine release (l'App doit avoir `contents: write` + `pull_requests: write` — c'est le cas si release-please tourne avec).
- [x] 2026-08-03 — DOCS: réécrire le `README.md` racine pour un lecteur qui n'est pas Kevin — restructuré autour des deux publics : « Try it » (umbrella `platform-deployment` en une commande `helm install`, avec la mention explicite du retard structurel d'une release de ses pins) et « Run it » (charts unitaires pinnés dans un repo privé + ApplicationSet, présenté comme le mode d'exploitation réel de l'auteur). Alignement avec la table « Direct / Infra repo » qui existait déjà dans `docs/getting-started.md`. Corrigés au passage : « Release Workflow » décrit désormais le vrai flux release-please (conventional commits → release PR → tag → `release-charts`, ligne 0.x assumée) au lieu du `git tag v1.0.0` manuel périmé ; l'exemple OCI passe de `targetRevision: "0.1.0"` à `"0.16.0"` avec la consigne « jamais une plage » ; la liste des charts dans l'arborescence passe de 5 à 10 (les vrais) ; badges release + licence ajoutés ; section License (Apache-2.0) ; la veille quotidienne d'advisories promue en feature visible.
- [x] 2026-08-03 — CHORE: évaluer si l'umbrella `platform-deployment` mérite d'être conservé — **conservé.** L'évaluation initiale (« personne ne le consomme ») mesurait la consommation contre le cluster de l'auteur ; le repo étant public et destiné à la réutilisation (énoncé le 2026-08-03), le consommateur pertinent est le nouveau venu, pour qui l'umbrella est la porte d'entrée en une commande. Les deux publics coexistent : umbrella pour découvrir, charts unitaires pour exploiter (la granularité est ce qui a permis de corriger Traefik seul, en 2 étapes contre 4 via l'umbrella). Conséquences : `umbrella-pins` garde sa raison d'être, l'automatisation du bump devient légitime (tâche dédiée), et le README racine présente désormais l'umbrella comme chemin « Try it ».
- [x] 2026-08-03 — CHORE(URGENT): ajouter un fichier `LICENSE` à la racine — **Apache-2.0**, choisi (vs MIT) pour l'alignement avec l'écosystème Kubernetes (k8s, Helm, ArgoCD, Traefik sont tous en Apache-2.0) et la clause de brevets qui protège les réutilisateurs. Texte canonique complet, appendix rempli (« Copyright 2026 Kevin De Benedetti »). Lève le blocage juridique de l'objectif « proposer le projet aux autres » : jusqu'ici `license: aucune` sur un repo public valait « tous droits réservés ». GitHub détectera la licence au push (badge sur la page du repo).
- [x] 2026-08-03 — CHORE: créer le secret `INFRA_READ_TOKEN` sur `k3s-lab` — PAT fine-grained en lecture seule (`Contents: Read-only`) sur `KevinDeBenedetti/infra`, posé via `gh secret set`. Validé en déclenchant `traefik-advisories` (run 30812703548) : job `watch` vert **sans aucune annotation** — le `::warning::` de secret absent a disparu, la section 2 (« version running in production ») a cloné `infra` et confronté le pin vivant aux advisories. La veille quotidienne couvre désormais les deux questions : ce que le repo déploierait *et* ce que le cluster résout. C'est ce contrôle-là qui aurait vu v3.7.1 gelée en production.
- [x] 2026-08-03 — FIX(URGENT): pousser le bump des pins de `infra/argocd/applicationsets/platform.yaml` — le constat du matin était bien pire que ce que le TODO croyait : **la production tournait sur `traefik v3.7.1`** (10 plages d'advisories, dont `GHSA-5r4w-85f3-pw66` HIGH bypass mTLS SNICheck ≤ v3.7.1 et `GHSA-x677-9fxg-v5c5` HIGH spoofing ForwardAuth ≤ v3.7.5), gelée depuis la purge de `0.9.1` sur GHCR : `origin/main` d'infra pinnait toujours 0.9.1, le bump du 2026-08-01 n'ayant jamais quitté le poste, et `platform-traefik`/`platform-external-secrets` étaient en `ComparisonError` (`Sync: Unknown`) — la panne silencieuse documentée dans le fichier lui-même. Résolu en deux pushes : `e114d35` (pins 0.9.1 → 0.15.0, dégel + v3.7.9 via pin) puis `a833692` (0.15.0 → 0.16.0, v3.7.9 natif). Vérifié de bout en bout sur le cluster : app-of-apps `root` synced, ApplicationSet à 0.16.0, les trois apps `Synced`/`Healthy`, pod `traefik:v3.7.9`, et `check-deployed-traefik.sh --applicationset` passe sur le fichier vivant (77 plages, propre). Leçon consignée : toute la veille interrogeait git et GHCR, jamais le cluster — c'est `kubectl` qui a révélé l'écart entre le pin « corrigé » localement et la réalité.
- [x] 2026-08-03 — FIX: `charts/platform-deployment` — remonter les 7 pins de sous-charts de 0.15.0 vers 0.16.0 (`b21ccf6`), suite au rouge attendu du job `umbrella-pins` sur la release PR #156 : la release 0.16.0 avait publié les sous-charts, les pins manuels de l'umbrella n'avaient pas suivi. `check-umbrella-pins.sh` repasse (7/7), CI verte sur `b21ccf6`.
- [x] 2026-08-03 — CHORE: republier `platform-traefik` — résolu par la release **v0.16.0** (2026-08-01 22:55 UTC, commit `ea6246c`), qui a embarqué `7a24f7b`. Vérifié en direct sur GHCR le 2026-08-03, les deux artefacts côte à côte : le 0.16.0 publié porte bien le subchart **41.1.0** (`Chart.yaml` + `Chart.lock`), **aucun `image.tag`** dans ses values, et l'`appVersion` du subchart embarqué est **v3.7.9** — il atteint donc la version saine nativement, par le chemin robuste, là où le 0.15.0 n'y arrivait que par son pin au-dessus d'un subchart en v3.7.6. `check-deployed-traefik.sh --chart-version 0.16.0` passe : 77 plages évaluées, proxy propre. La divergence source/artefact est fermée. Reste le pin de l'ApplicationSet (0.15.0 → 0.16.0), ré-ouvert en tâche dédiée ci-dessus.
