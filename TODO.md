# TODO

## 🔴 En cours

## 🟡 À faire


## 🟡 À faire (manuellement, étapes détaillées pas à pas)
- [ ] CHORE: armer le `schedule:` de `.github/workflows/kyverno-crds.yml` — **la moitié k3s-lab est faite (2026-08-19)**, il ne reste que la dépendance infra. Le workflow cible désormais `runs-on: k3s-lab` (label déclaré dans `.github/actionlint.yaml`), n'utilise plus aucun secret `KUBECONFIG` — le runner tournant dans le cluster, `kubectl` s'authentifie via le ServiceAccount du pod — et installe `kubectl` lui-même, l'image ARC ne le fournissant pas contrairement à `ubuntu-latest`. Le `schedule` reste **commenté** parce qu'un job planifié visant un runner non enregistré n'échoue pas : il reste *queued* jusqu'au timeout de 6 h, tous les jours — plus silencieux qu'un rouge. Débloquer dans l'ordre : 1) créer le scale set ARC `k3s-lab` côté infra (entrée dédiée dans `infra/TODO.md`, manifeste RBAC prêt) ; 2) lancer une fois en `workflow_dispatch` et vérifier que le job démarre, que `kubectl version --client` passe et que le check lit bien les policies ; 3) seulement ensuite décommenter le `cron`.
- [ ] TEST: le job CI `k3s-lab-pin` n'a jamais tourné (ajouté depuis un sandbox sans accès Actions) — surveiller la première exécution : si l'image du runner n'a pas `git`, `actions/checkout` bascule sur le tarball et l'assertion échouera volontairement. NB 2026-08-17 : le runner est l'image officielle `ghcr.io/actions/actions-runner:2.335.1`, et l'étape `Assert the submodule is really checked out` fait échouer le job avec un message explicite plutôt que de passer vert à vide — le mode de défaillance est donc bruyant. Reste à confirmer sur un run réel.
- [ ] TEST: aucun check ne relie un CronJob à la version d'app qui expose l'endpoint qu'il appelle — le purge tapait une route absente de l'image depuis sa création, et rien n'a rien dit pendant des semaines. ⚠ 2026-08-17 : **hors de ce dépôt** — `grep -rl "kind: CronJob" charts/ kubernetes/` ne renvoie rien, il n'y a aucun CronJob ici. Le CronJob `portfolio/purge` et le Deployment dont il faudrait comparer la version vivent tous les deux dans le dépôt infra ; le check doit y être écrit.
- [ ] CHORE: migrer `infra/platform/security/values.yaml:51` de `kyvernoPolicies.validationFailureAction` vers `failureAction`, puis retirer l'alias déprécié du helper — l'alias n'existe que pour ce consommateur, et le test « the deprecated validationFailureAction value key still overrides » est là pour rendre sa suppression délibérée. ⚠ 2026-08-17 : **bloqué par ordonnancement inter-dépôts, et c'est voulu** — `charts/platform-security/templates/_helpers.tpl:41` dit noir sur blanc « Remove the alias only once infra sets `failureAction` ». Il n'y a pas de `infra/` ici : la première moitié (migrer le values infra) doit être faite dans l'autre dépôt et déployée AVANT que l'alias puisse tomber ici. Retirer l'alias maintenant casserait le consommateur en production.


## 🟢 Idées / backlog

## 🤖 Claude — recommandations
- [ ] CHORE: infra épingle `platform-security` à `0.18.4` alors que `0.19.0` est publiée — 1 release de retard, sous le seuil de 3 donc non signalé, mais c'est le seul des 4 charts non aligné et l'écart contient le refactor `failureAction`
- [ ] TEST: rien n'empêche le retour du piège `[ … ] && cmd` en fin de corps de boucle dans un pipeline capturé — shellcheck ne l'attrape pas, et il a coûté une issue ouverte pour une fausse dérive ; un grep de forme dans les checks CI serait peu coûteux

## ✅ Fait

- [x] 2026-08-19 — FIX: issue #76 — **ce n'était pas une dérive de pins mais un bug de ce dépôt**. Le corps de l'issue montre une section 3 réduite à sa ligne d'en-tête, sans verdict : `check-deployed-pins.sh` mourait en silence. Dans la substitution `newer=`, le corps de boucle finissait par `[ … ] && printf` ; sur tout tag *pas* plus récent que le pin ce corps rend non-zéro, le `while` en hérite, `pipefail` le propage au pipeline et `set -e` tue le script sur l'affectation **sans rien afficher**. Déclencheur cruel : ça ne casse que lorsqu'AUCUN tag n'est plus récent, c'est-à-dire quand un pin **rattrape** la dernière release — le veilleur de dérive s'est cassé au moment exact où la dérive était corrigée, et l'a signalé comme une dérive. Corrigé en `if/then`, + `tests/bats/deployed-pins.bats` (7 tests, registre stubbé par un faux `curl`, donc hors ligne) vérifiés non vacueux contre la forme buguée. Balayage : seule occurrence de cette forme dans un pipeline capturé. L'issue se refermera d'elle-même au prochain run vert du workflow
- [x] 2026-08-19 — CHORE: exempter le namespace `ci` des ClusterPolicies — **portée réduite par rapport à l'énoncé, après vérification** : les 6 policies ne partagent pas une liste d'exclusion, elles en ont 6 différentes (2 à 8 namespaces) ; les « 8 namespaces » de la tâche ne décrivent que `require-ro-rootfs`. Les unifier aurait élargi cinq policies d'un coup, en silence. `ci` n'a donc été ajouté qu'à `require-ro-rootfs` et `restrict-capabilities`, les deux que les pods ARC violent réellement d'après la note infra — même portée que la PolicyException qu'elle remplace. 4 tests verrouillent les deux moitiés : présent là où il faut, **absent** des quatre autres, listes toujours distinctes
- [x] 2026-08-19 — TEST: `check-play-order.sh` vérifie désormais la **couverture d'hôtes** en plus de l'ordre — le play `ufw_enable` doit couvrir tout pattern d'hôtes ayant reçu des règles (`all` court-circuite, unions `:` gérées, comparaison conservatrice faute d'inventaire en CI). Vérifié sur le scénario exact de la tâche : un play final en `hosts: k3s_servers` laisse `k3s_agents` non couvert et échoue en le nommant. 4 tests ajoutés (14 au total)
- [x] 2026-08-19 — CHORE: `scripts/check-inventory-ignored.sh` + `tests/bats/inventory-ignored.bats` (9 tests), câblé en CI. **La prémisse était fausse et la situation pire** : l'inventaire n'était pas « gitignored sans garde-fou », il n'était **pas ignoré du tout** — la seule entrée Ansible de `.gitignore` était `ansible/*.retry`. Les fichiers générés par `generate-inventory.sh` (IP publiques ET privées des nœuds) étaient *non suivis mais non ignorés* : invisibles à tous les checks et à un `git add -A` d'être publiés. Ajout de la règle `ansible/inventory/`, puis du garde-fou vérifiant les deux moitiés — rien de suivi (un `.gitignore` ne s'applique pas rétroactivement) et règles toujours actives — avec recoupement contre `generate-inventory.sh` pour ne pas garder un chemin que plus personne n'écrit
