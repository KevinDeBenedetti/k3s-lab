# =============================================================================
# Traefik Dashboard — secured via VPN + BasicAuth + HTTPS
# Prerequisites:
#   1. Create the BasicAuth secret (run once on your local machine):
#      kubectl create secret generic traefik-dashboard-auth \
#        --from-literal=users="$(htpasswd -nb admin <YOUR_PASSWORD>)" \
#        -n ingress
#   2. Set DASHBOARD_DOMAIN in your .env and replace below.
# =============================================================================

# -- Middleware: BasicAuth for dashboard access
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: dashboard-basicauth
  namespace: ingress
spec:
  basicAuth:
    secret: traefik-dashboard-auth
    removeHeader: true

---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: dashboard-redirect-scheme
  namespace: ingress
spec:
  redirectScheme:
    scheme: https
    permanent: true

# -- Middleware: VPN-only (scoped to ingress namespace)
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: vpn-only
  namespace: ingress
spec:
  ipAllowList:
    sourceRange:
      - 10.8.0.0/24   # WireGuard tunnel — only reachable when VPN is connected

# -- IngressRoute: expose dashboard on dashboard.<DOMAIN>
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: ingress
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`${DASHBOARD_DOMAIN}`) && (PathPrefix(`/dashboard`) || PathPrefix(`/api`))
      kind: Rule
      middlewares:
        - name: vpn-only
          namespace: ingress
        - name: dashboard-basicauth
          namespace: ingress
      services:
        - name: api@internal
          kind: TraefikService
  tls:
    secretName: traefik-dashboard-tls

# -- Certificate for the dashboard domain
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-dashboard-tls
  namespace: ingress
spec:
  secretName: traefik-dashboard-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - ${DASHBOARD_DOMAIN}
